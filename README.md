# ai-prompt-library
AI Prompt Library is a full-stack app for managing AI image prompts using Angular and Django. It uses PostgreSQL for storage and Redis for real-time view counting. Users can create, view, and browse prompts with validated forms. The system is containerized using Docker Compose for easy setup and scalable deployment.
